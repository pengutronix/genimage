image test.verity-hash {
	verity {
		image = "test.verity-data"
	}
}

image test.verity-sig {
	verity-sig {
		image = "test.verity-hash"
		cert = "signing-ca/signing.cert.pem"
		key = "signing-ca/signing.key.pem"
	}
}
